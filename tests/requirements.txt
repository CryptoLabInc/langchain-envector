# Test dependencies (pin exact versions for reproducibility)

# Test runner
pytest
pytest-asyncio

# pyenvector SDK. 1.6.0rc1 is a pre-release, but naming it in the specifier is
# enough for pip to consider it — no --pre needed.
pyenvector>=1.6.0rc1

# Note: LangChain is optional for tests. Integration tests will fall back to
# sentence-transformers if LangChain embeddings are unavailable.

# Hugging Face datasets (optional: integration tests and utilities)
datasets

# Sentence-Transformers for real embeddings in integration tests
sentence-transformers

# PyTorch; required by sentence-transformers (CPU wheels will be resolved per platform)
torch

# LangChain core and test utilities (local installs from repo root)
langchain
langchain-core
langchain-tests
