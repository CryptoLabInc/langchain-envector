# Data

This folder can contain sample datasets for local testing.

Generated dataset
- Run `python scripts/make_synthetic_rag_dataset.py` to create `synthetic_rag_1k.jsonl`.
- Each line is a JSON object with `text` and `metadata` fields.

